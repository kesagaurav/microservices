server:
  port: 8081
spring:
  application:
    name: HotelService
  cloud:
    consul:
      discovery:
        hostname:localhost
      config:
        enabled: true
        prefix: config
        defaultContext: applications
        profileSeparator: '::'
        format: YAML