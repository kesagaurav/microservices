server:
  port: 8082
spring:
  application:
    name: RatingService
  cloud:
    consul:
      discovery:
        hostname:localhost
      config:
        enabled: true
        prefix: config
        defaultContext: applications
        profileSeparator: '::'
        format: YAML